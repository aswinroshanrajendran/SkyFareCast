# SkyFareCast
A Python-based machine learning project to predict flight fares using historical data. Includes end-to-end pipeline with EDA, feature engineering, model training using Random Forest Regressor, and a Flask web deployment for real-time predictions.
